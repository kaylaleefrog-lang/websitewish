
  # Wishlist app with notifications

  This is a code bundle for Wishlist app with notifications. The original project is available at https://www.figma.com/design/lNiIFTa67SEOyUM3L4L7Ca/Wishlist-app-with-notifications.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  